apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qa-ingress
  namespace: qa
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: qa-ingress
  rules:
  - host: qa.localhost.com
    http:
      paths:
      - path: "/user"
        pathType: Prefix
        backend:
          service:
            name: gf-user-service
            port:
              number: 80
  tls: 
  - hosts: 
      - qa.localhost.com
    secretName: tls-secret